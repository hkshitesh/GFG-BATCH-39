---
- hosts: gfg
  become: yes
  vars:
    operation: install   # change to 'remove' to uninstall packages

  tasks:

    - name: Update apt cache
      apt: update_cache=yes

    - name: Install htop
      action: yum name=htop state=present update_cache=yes
      when: operation == "install"

    - name: Install git
      action: yum name=git state=present update_cache=yes
      when: operation == "install"

    - name: Remove htop
      action: yum name=htop state=absent
      when: operation == "remove"

    - name: Remove git
      action: yum name=git state=absent
      when: operation == "remove"

    - name: Autoremove unused packages
      action: yum autoremove=yes
      when: operation == "remove"
